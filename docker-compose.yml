version: "3.8"
services:
  mongo:
    image: mongo:6.0
    container_name: my_mongo
    ports:
      - "27017:27017"
  
  db_manager:
    build: .
    depends_on:
      - mongo
    ports:
      - "8081:8081"
    environment:
      - MONGO_URI=mongodb://mongo:27017
